{
    "version": 2,
    "builds": [
      {
        "src": "app.js",  
        "use": "@vercel/node",
        "config": { "start": "node ./bin/www" }  
      }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/index.js" }]
  }